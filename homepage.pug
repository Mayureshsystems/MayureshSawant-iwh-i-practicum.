html
     head 
         meta(name="viewport" content="width=device-width, initial-scale=1")
         link(rel="stylesheet", href="/css/style.css")
     body    
         h1 Custom object table
         a(href="/update-cobj") Add/Update this table
         table(style='width:80%;margin:2%', border='1')
                tr 
                    th make
                    th model
                    th year
                    th vin
                each tree in data
                    tr 
                        td #{tree.properties.make}  
                        td #{tree.properties.model}
                        td #{tree.properties.year}
                        td #{tree.properties.vin}